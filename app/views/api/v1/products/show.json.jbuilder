json.extract! product, :id, :name, :duration, :length, :difficulty, :height, :description, :departure_place, :period
# json.comments @restaurant.comments do |comment|
#   json.extract! comment, :id, :content
# end